# ToDoapp

This is a simple To-Do application designed for students and teachers (Admin). Before starting the application, you must update the application.properties file by setting the database URL, username, and password according to your MySQL configuration in order to establish a successful database connection.
